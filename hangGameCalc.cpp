#include <iostream>
#include <string>
#include <vector>
#include <conio.h>
#include <_mingw.h>
#include <stdlib.h>
#include <time.h> 
using namespace std;

void printGame(int fails, string word, string failLetters)
{
	switch (fails)
	{
		case 0:
			string man = |
		         |
		         |
			=========
			"""
			break;
	}
}

void hangGamePvE()
{
	int fails = 0;
	string failLetters = "";

	srand(time(NULL));
	string possibleWords[10] = {"word", "potato", "beach", "summer", "mouse", "horse", "keyboard", "key", "paper", "phone"};
	string computerElection = possibleWords[rand() % 10];
	char completed[computerElection.length()];


	for (int i = 0; i < computerElection.length(); i++)
	{
		completed[i] = ['_'];
	}



}
